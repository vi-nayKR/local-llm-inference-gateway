version: '3.8'

services:
  redis-stack:
    image: redis/redis-stack:latest
    container_name: gateway_redis_vector
    ports:
      - "6379:6379"
      - "8001:8001"
    environment:
      - REDIS_ARGS=--save 60 1000
    volumes:
      - redis_data:/data

volumes:
  redis_data:
